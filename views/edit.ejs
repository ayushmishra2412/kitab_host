<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-8 text-white">
        <div class="w-full flex justify-between mb-4">
            <h2 class="text-xl text-center text-red-200">KITAB 📕</h2>
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-1 inline-block">Logout</a>
        </div>
        <h5 class="mb-1">Edit Your Post:</h5>
        <form action="/update/<%= post._id%>" method="post">
            <textarea class="block w-1/3 p-3 outline-none resize-none bg-transparent border-2 rounded-md border-zinc-800" placeholder="<%=post.content%>" name="content"></textarea>
            <input type="submit" value="Update Post" class="px-3 py-2 text-sm w-30 bg-blue-500 block rounded-md mt-2">
        </form>
    </div>
</body>
</html>